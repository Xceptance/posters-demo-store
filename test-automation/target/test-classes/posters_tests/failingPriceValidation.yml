prompt: |
  Open ${neodymium.url}
  Search for '${searchTerm}'.
  Open a product
  Verify that the product price is exactly '${expectedPrice}'.

data:
  - testId: "failingPriceValidation_case1"
    searchTerm: "Boston"
    expectedPrice: "$0.00"
