## 1. Refactor detail.html

- [x] 1.1 Update the `detail.html` layout structure to match the standard card pattern (e.g., proper use of `bo-card` classes with enhanced margins/padding).
- [x] 1.2 Improve typography and visual hierarchy using `fw-bold`, `text-muted`, and proper headings.
- [x] 1.3 Move the "Back to List" action to standard page actions area if needed, or adjust its presentation.

## 2. Refactor Fragments

- [x] 2.1 Update `profile-name-display.html` to align with the new card aesthetic (adjust padding/margins and button styling).
- [x] 2.2 Update `profile-name-form.html` to match the new layout and ensure inline editing still functions perfectly.
- [x] 2.3 Refactor `address-list.html` to use the dropdown pattern for secondary actions (Delete) and improve visual presentation of the address data.
- [x] 2.4 Refactor `credit-card-list.html` to use the dropdown pattern for secondary actions (Delete) and improve visual presentation of the card data.
- [x] 2.5 Ensure all fragments maintain their `id` attributes for HTMX targeting.

## 3. Verify

- [x] 3.1 Verify the layout matches the premium aesthetic across the detail view.
- [x] 3.2 Verify all HTMX interactions (adding/editing/deleting addresses and credit cards, editing profile name) function correctly.
- [x] 3.3 Ensure the layout is responsive on smaller screens.
